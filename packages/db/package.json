{
  "name": "@enrichment-saas/db",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "migrate:up": "node-pg-migrate -m src/migrations up",
    "migrate:down": "node-pg-migrate -m src/migrations down"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "@enrichment-saas/contracts": "workspace:*"
  },
  "devDependencies": {
    "node-pg-migrate": "^6.2.2",
    "@types/pg": "^8.11.0"
  }
}
